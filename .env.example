# LocAI Environment Variables
# Copy this file to .env.local and adjust values as needed.

# Ollama host URL (default: http://localhost:11434)
# NEXT_PUBLIC_OLLAMA_URL=http://localhost:11434

# API token for securing API routes (optional, but recommended if exposed)
# LOCAI_API_TOKEN=your-secret-token-here

# Allow remote requests to API routes (default: false, localhost-only)
# LOCAI_ALLOW_REMOTE=false

# Default base path for file-based notes storage
# LOCAL_NOTES_PATH=/path/to/your/notes
